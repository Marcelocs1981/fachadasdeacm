<section class="hero">
  <div class="container">
    <div class="hero-content">
      <h1>Fachadas em ACM</h1>

      <p>
        Projetos modernos com acabamento de alto padrão para empresas que valorizam imagem e durabilidade.
      </p>

      <a href="#contato" class="btn">
        Solicitar orçamento
      </a>
    </div>
  </div>
</section>

<style>
.hero {
  background: #7c3aed;
  padding: 120px 20px; /* mais respiro */
  color: #fff;
}

.hero-content {
  max-width: 520px;
}

/* TÍTULO */
.hero h1 {
  font-size: 48px;
  font-weight: 300; /* mais elegante */
  letter-spacing: -0.5px; /* mais sofisticado */
  line-height: 1.15;
  margin-bottom: 20px;
}

/* TEXTO */
.hero p {
  font-size: 17px;
  line-height: 1.6;
  opacity: 0.85;
  margin-bottom: 36px;
  font-weight: 400;
}

/* BOTÃO */
.btn {
  display: inline-block;
  background: transparent;
  color: #fff;
  padding: 12px 26px;
  border-radius: 999px; /* botão premium */
  font-size: 13px;
  font-weight: 500;
  letter-spacing: 1px;
  text-transform: uppercase;
  border: 1px solid rgba(255,255,255,0.6);
  transition: all 0.3s ease;
}

.btn:hover {
  background: #fff;
  color: #000;
  border-color: #fff;
}
</style>